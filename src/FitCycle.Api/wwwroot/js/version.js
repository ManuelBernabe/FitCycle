// FitCycle App Version — bumped on every meaningful release.
//
// Update this string ONLY when shipping a user-visible change (parser fix, workout
// feature, UI rework). Internal-only refactors that don't affect the user shouldn't
// bump it. The header in app.js and the home page both read this value.
//
// Versioning style is semver-ish:
//   MAJOR — breaking UX changes (auth, data model, etc.)
//   MINOR — new pages, features, or notable PDF parser changes
//   PATCH — small fixes, polish, copy
export const APP_VERSION = '1.4.8';
